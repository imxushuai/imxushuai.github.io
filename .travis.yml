language: node_js
node_js: 
  - 12.16.3

# Travis-CI Caching
cache:
  directories:
    - node_modules

# S: Build Lifecycle
install:
  - npm install

before_script:
# - npm install -g gulp
#  - npm install -g hexo-cli
#  - npm install hexo --save


script:
  - hexo clean && hexo algolia && hexo g
  - cd ./public
  - git init
  - git config user.name "imxushuai"
  - git config user.email "imxushuai@gmail.com"
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master
  - git push --quiet "https://qinyuanpei:${CO_TOKEN}@${CO_REF}" master:master --tags


branches:
  only:
    - hexo
env:
  global:
    - GH_REF: github.com/imxushuai/imxushuai.github.io.git
    - CO_REF: git.coding.net/imxushuai-01/imxushuai.coding.me.git
